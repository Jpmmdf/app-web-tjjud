apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: app-web-tjjud-demo-tls
spec:
  secretName: app-web-tjjud-demo-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-cloudflare
  dnsNames:
    - front-demo.pavim.com.br
